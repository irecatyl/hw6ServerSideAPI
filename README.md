# hw6ServerSideAPI
This is how webpages get linked together
The end tag of the URL links the pages to one another.
The END of the URL will access the desired database
The code is written to call this API by using the web browser address bar to call the data needed for the page to populate.
